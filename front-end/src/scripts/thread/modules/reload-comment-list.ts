import { Comment } from './../../_interfaces/Comment';

export const reloadCommentList = (pageList, currentPage: {number: number}, pagesInfo: {pages: number, lastPageThreads: number}, threadList: Comment[]) => {
    return () => {
        
    }
};