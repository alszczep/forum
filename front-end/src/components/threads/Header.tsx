import { FC } from 'react';

const Header: FC= (): JSX.Element => {
    
    return (<section className='headerWrapper'>
        <h1 className='threadsHeader'>1</h1>
        <button className='newThreadButton'>new thread</button>
    </section>)
}

export default Header;